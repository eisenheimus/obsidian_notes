##### Приложение запущено, в директории файл удален

```bash
# Узнаем PID приложения
ps aux | grep [приложение]

# По PID смотрим все открытые файлы и узнаем файловый дискриптор
lsof -p [PID]

# сохраняем конфиг из proc
cat /proc/[PID]/fd/[FD] > ./app.conf
```
