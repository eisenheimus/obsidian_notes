
|   |   |   |
|---|---|---|
|`traceroute`|`traceroute`, `tracepath`|Трассировка маршрута до хоста|
|`mtr`|`mtr`|Комбинация `ping` + `traceroute` в реальном времени (очень удобно!)|
|`net-tools`|`ifconfig`, `netstat`, `route`, `arp`|Классические утилиты (устаревают, но ещё используются)|
|`iproute2`|`ip`, `ss`, `tc`|Современная замена `net-tools` (обычно уже установлен)|
|`ethtool`|`ethtool`|Настройка параметров сетевого интерфейса (скорость, дуплекс)|
|`whois`|`whois`|WHOIS-запросы к регистраторам доменов|

---

### 📡 Продвинутая диагностика и анализ трафика

|Пакет|Утилиты|Назначение|
|---|---|---|
|`nmap`|`nmap`|Сканирование портов, обнаружение хостов, определение ОС|
|`tcpdump`|`tcpdump`|Захват и анализ сетевого трафика (требует root)|
|`tshark`|`tshark`|Консольная версия Wireshark (анализ пакетов)|
|`netcat`|`nc`|«Швейцарский нож» для работы с TCP/UDP соединениями|
|`socat`|`socat`|Продвинутый `netcat` с поддержкой TLS, проксирования и т.д.|
|`arp-scan`|`arp-scan`|Сканирование локальной сети через ARP|

---

### 📊 Мониторинг трафика в реальном времени

|Пакет|Утилиты|Назначение|
|---|---|---|
|`iftop`|`iftop`|Мониторинг трафика по соединениям (как `top` для сети)|
|`nethogs`|`nethogs`|Группировка трафика по процессам|
|`bmon` / `vnstat`|`bmon`, `vnstat`|Статистика использования трафика с историей|

---

### 🧪 Тестирование производительности сети

| Пакет           | Утилиты     | Назначение                                        |
| --------------- | ----------- | ------------------------------------------------- |
| `iperf3`        | `iperf3`    | Тестирование пропускной способности между хостами |
| `speedtest-cli` | `speedtest` | Замер скорости интернета через Speedtest.net      |