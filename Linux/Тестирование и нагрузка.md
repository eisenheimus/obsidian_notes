
Имитация нагрузки процессора
```
stress-ng --cpu 8 --cpu-method matrixprod
```

Имитация большого файла
```bash
sudo mkdir -p /mnt/test_io

# if=/dev/zero — источник данных: специальное устройство, которое выдает неограниченный поток нулевых байтов.
# of=/mnt/test_io/testfile — путь, куда будет записываться файл.
# bs=1M — размер блока для чтения и записи: 1 мегабайт.
# count=51200 — количество блоков: 51 200.
# oflag=direct — запись минует файловый кэш операционной системы и идет напрямую на диск, что позволяет более точно измерять производительность дисковой подсистемы. диск, что позволяет более точно измерять производительность дисковой подсистемы.
dd if=/dev/zero of=mnt/test_io/testfile bs=1M count=51200 oflag=direct

```